describe('TextLayout', () => {

    describe('getByteStack', () => {
        it('should return a single number composed of bytes from the same x position of all pages', () => {

        });
    });
});